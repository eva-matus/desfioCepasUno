<%= form_with(model: wine_strain, local: true) do |form| %>
  <% if wine_strain.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(wine_strain.errors.count, "error") %> prohibited this wine_strain from being saved:</h2>

      <ul>
      <% wine_strain.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>



  <div class="field">
    <%= form.label :percentage %>
    <%= form.text_field :percentage %>
  </div>

  <div class="field">
    <%= form.label :wine_id %>
    <%= form.text_field :wine_id %>
  </div>

  <div class="field">
  <%= form.label :strains %>
  <%= form.collection_check_boxes(:strain_id, Strain.all, :id, :name) do |strain| %>
    <%= strain.label class: "label-checkbox" do %>
      <%= strain.check_box  + strain.text%>
    <% end %>
  <% end %>
</div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
